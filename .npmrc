inject-workspace-packages=true

# Suppress peer dependency warnings for packages that work with NestJS 11
# but haven't updated their peer dependency declarations yet
strict-peer-dependencies=false
auto-install-peers=true
